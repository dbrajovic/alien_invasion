package main

import (
	"fmt"
	"log"
)

func main() {
	var input string

	if _, err := fmt.Scanf("%d", &input); err != nil {
		log.Fatal("failed to read input: %w", err)
	}

	//	init game

	//	run game
}
